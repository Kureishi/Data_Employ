pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
sqlalchemy>=2.0
joblib>=1.3
tabulate>=0.9
flask>=3.0
# Production WSGI server (cross-platform, thread-pool based). Used by web_api
# as the default server and referenced by wsgi.py. Optional on Linux where
# gunicorn is preferred instead.
waitress>=3.0
# Optional: shared Redis for rate limiting + session registry across multiple
# worker processes / nodes (MLAGENT_REDIS_URL). Without it, the API transparently
# falls back to per-process (in-memory / local-SQLite) backends.
# redis>=5.0
